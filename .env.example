# Required — Get this from Neon.tech dashboard
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# App URL — used for SSR metadata and API calls
NEXT_PUBLIC_APP_URL="http://localhost:3000"
